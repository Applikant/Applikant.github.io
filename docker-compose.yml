services:
  docs:
    image: squidfunk/mkdocs-material
    command: serve --dev-addr=0.0.0.0:8000 --watch-theme --dirty
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true

